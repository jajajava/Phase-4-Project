class GardensController < ApplicationController
    # def index
    #     garden = Garden.create!(user_id: user.id, event_id: INTERPOLATE EVENT ID )
    # end

    # private
    
    # params.permit(:event_id) Depending on how I interpolate the id, I might not need this at all
end
